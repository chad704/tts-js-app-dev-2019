//Exercise 1

/*let list = ['pop tarts', 'ramen noodles', 'chips', 'salsa', 'coffee'];

list.push('fruit loops');
list[4] = 'fair trade coffee';
list.splice(2,2,'rice','beans');
var cart = [];
cart.unshift(list.pop());
cart.push(list.shift());

while (list.length > 0) {
    cart.push(list.pop());
};

cart.reverse(cart.sort());
cart.toString(cart.join(','));

console.log(cart);
console.log(list);

//Exercise 2 Addressing Objects

1. course.name = 'Javascript Applications'
2. course.teachers[1] = 'Shane'
3. course.students[0].name = 'Steve'
4. course.students[1].computer.type = 'macbook'
5. course.preReqs.equipment
6. course.preReqs.equipment.OSOptions[1] = 'osx'
7. course.preReqs.equipment.OSOptions.join(' or ');*/
8. const studentsOSX = [course.students[1].name,course.students[2].name];